<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:layout_margin="@dimen/task_item_padding"
    android:padding="@dimen/task_item_padding"
    android:background="@drawable/border_layout"
    >
    <CheckBox
        android:id="@+id/task_item_done"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:buttonTint="?secondaryInnerColor"
        />
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_weight="0.7">
        <TextView
            android:id="@+id/task_item_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="?secondaryInnerColor"
            android:paddingStart="@dimen/task_item_padding"
            android:paddingTop="@dimen/task_item_padding"
            android:paddingEnd="@dimen/task_item_padding"
            android:paddingBottom="@dimen/task_item_padding"
            android:textSize="18sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            />

        <TextView
            android:id="@+id/task_item_date"
            android:layout_width="match_parent"
            android:textColor="?secondaryInnerColor"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/task_item_padding"
            android:paddingTop="@dimen/task_item_padding"
            android:paddingEnd="@dimen/task_item_padding"
            android:textSize="16sp" />

    </LinearLayout>

    <ImageView
        android:id="@+id/icon_image_view"
        android:layout_width="40dp"
        android:layout_height="match_parent"
        tools:srcCompat="@drawable/ic_house" />
</LinearLayout>